<!--<h1>Shopes#index</h1>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Brand</th>
      <th>Description</th>
      <th>Price</th>
      <th>Product_Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.brand %></td>
        <td><%= product.description %></td>
        <td><%= product.price %></td>
        <td><%= image_tag product.avatar, width: "200px" if product.avatar.attached?  %></td>
        <td><%= link_to 'Show', shope_path(product.id) %></td>

       
      </tr>
    <% end %>
  </tbody>
</table>

<br>-->
<hr>
  <% if current_user.role == 'admin' %>
   <%= link_to 'Add New Product',new_product_path, class: "btn" %>
   <%= link_to 'Go to orders ', lists_path, :class => 'btn btn-info btn-lg btn-block' %>

  <% end %>
  
   
<div class="product-index-grid pt4">
  <% @products.each do |product| %>

    <div class="product border-light">
      <div class="product-thumb">
      <%= image_tag product.avatar, width: "200px" if product.avatar.attached?  %>
     
      </div>
       <div class="pa3">

      <h3 class="fw7 f4 title"><%= product.brand %></h3>
      <hr>
      <p class="f3 fw6 has-text-right pt2 price"><%= number_to_currency(product.price) %></p>
    </div> 
    <%= truncate(product.description, length: 40) %><br>
    <br>
            <span>
              
              <h1> </h1>
            </span>

        <%= link_to 'Show', shope_path(product.id), class:"button is-warning" %>
        <br>
            <span>
              
              <h1> </h1>
            </span>

    <% if current_user.role == 'admin' %>
    <br>
            <span>
              <h1> </h1>
            </span>
          
    
            <span><%= button_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' }, class:"button is-danger" %></span>
            <br>
            <span>
              <h1> </h1>
            </span>
          
            <span><%#= link_to 'Issue', book_path(book) %></span>
            <% end %>
  </div>

  
  <% end %>
  <br>
            <span>
              
              <h1> </h1>
            </span>
</div>



